import React from 'react';

const Home = (props) => {
    const style = {fontWeight: "bold", fontSize: "100px", backgroundColor: "lightgreen"}
    return (
        <div>
        <h1 style={style}>Welcome!</h1>
        </div>
    );
}

export default Home;